fix: USP-01 silent money bug + genesis text + build.rs + Tor docs

usp01.rs (CRITICAL - 8 instances):
- checked_sub().unwrap_or(0) -> match with error return
  (was silently destroying tokens on underflow)
- checked_add().unwrap_or(u128::MAX) -> match with error return
  (was silently creating infinite balance on overflow)
- Affects: Transfer, TransferFrom, Burn, WrapMint, WrapBurn

genesis/src/main.rs:
- "balance >= 1000 LOS" -> "registers with >= 1 LOS (rewards >= 1000 LOS)"
  (spec: MIN_VALIDATOR_REGISTER_CIL = 1 LOS, not 1000)

flutter_wallet/TOR_IMPLEMENTATION.md:
- Remove hardcoded "Tor 0.4.8.22" version
- Remove "macOS binary" platform assumption
- Use runtime version check instead

pqcrypto-internals-seeded/build.rs (3 copies):
- unwrap() -> descriptive expect() messages
- Root, flutter_wallet, flutter_validator copies all fixed

Build: 0 warnings, 0 errors
Tests: 282 passed, 0 failed, 4 ignored
