fix: zero-defect audit - eliminate all panics, unsafe env, dangling pointers

Fixes applied across 7 files:

CLI (los-cli/src/main.rs):
- Banner version: hardcoded v0.1.0 -> env!(CARGO_PKG_VERSION) dynamic
- Utility functions: restore pub(crate) visibility (used by command submodules)

Node (los-node/src/main.rs):
- --port parse: unwrap_or(3030) -> match with eprintln warning
- --config port parse: same silent default -> match with warning
- --mine-threads parse: unwrap_or(1) -> match with warning
- send amount parse: unwrap_or(0) -> match with distinct error messages
- std::env::set_var: all 3 calls wrapped in unsafe {} (Rust 1.83+)

Tor (tor_service.rs, tor_transport.rs):
- All set_var/remove_var in tests wrapped in unsafe {} blocks

Example contracts (hello_world.rs, oracle_price_feed.rs, token.rs):
- Dangling pointer: added std::mem::forget(output) before returning ptr
- expect() panics: replaced with match + graceful error_response()
- unwrap() calls: replaced with .ok() returning Option
- token.rs get_state(): expect() -> auto-initialize empty state

Build: 0 warnings, 0 errors
Tests: 282 passed, 0 failed, 4 ignored
